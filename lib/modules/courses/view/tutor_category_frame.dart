import 'package:flutter/material.dart';

class TutorCategoryFrame extends StatelessWidget {
  const TutorCategoryFrame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TutorCategoryFrame'),
    );
  }
}
